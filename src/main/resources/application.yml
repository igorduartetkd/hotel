spring:
  datasource:
    url: jdbc:h2:file:./hotel_db
    username: usuario-maneiro
    password: senha-muito-dificil
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create